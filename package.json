{
  "name": "freqtrade-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "console"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=console",
    "build": "npm run build --workspace=console",
    "start": "npm run start --workspace=console",
    "lint": "npm run lint --workspace=console"
  }
}
