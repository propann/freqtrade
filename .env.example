# Domaine public utilisé par Nginx pour le portail (ex: example.com)
PUBLIC_DOMAIN=example.com

# Port HTTP local du portail (bind sur 127.0.0.1 uniquement)
PORTAL_HTTP_PORT=8088

# Jeton signé pour le portail (placeholder, à remplacer en prod)
PORTAL_JWT_SECRET=change-me-in-prod

# Base Postgres utilisée par le portail (identifiants placeholders)
POSTGRES_DB=freqtradeaws
POSTGRES_USER=freqtradeaws
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

# Nom du réseau Docker partagé par les services de contrôle
DOCKER_NETWORK=freqtrade-aws-net

# Répertoire racine où seront créés les environnements clients
CLIENTS_DIR=./clients

# Répertoire des logs agrégés
LOGS_DIR=./logs

# Sécurité : ne jamais commiter de vrais secrets. Copier ce fichier en `.env` et ajuster localement.
# Maintenir le portail en DRY-RUN : ne jamais activer les clés API réelles pour les clients dans le MVP.
