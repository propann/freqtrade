risk_limits:
  max_concurrent_pairs:
    min: 1
    max: 20
  max_drawdown_percent:
    min: 0
    max: 50
  max_daily_trades:
    min: 1
    max: 200
quotas:
  default_cpu: 1.0
  default_memory: 1024m
  default_pids: 256
  max_bots_per_tenant: 5
